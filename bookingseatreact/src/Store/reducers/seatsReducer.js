const DEFAULT_STATE = {
    selectedSeat: 'true',
};
const type = '';
export const seatsReducer = (state = DEFAULT_STATE) => {
    switch (type) {

        default:
            return state;
    }
};
